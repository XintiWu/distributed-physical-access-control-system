第一張圖：單元測試 (Unit Testing) ——「核心邏輯防線」

單元測試的本質是「把外部元件（如資料庫、Kafka）切斷，只測試純程式碼的邏輯」。在簡報上，你要畫出一張 「Mock（虛擬隔離）測試架構圖」。
1. 你的簡報圖應該這樣畫：

你可以用簡單的方塊與虛線，畫出你們在第三階段做的「測試衝刺」架構：

 ┌────────────────────────────────────────────────────────┐
 │                   [ 核心邏輯單元測試 ]                 │
 │                                                        │
 │  ┌──────────────┐         IsDenied?       ┌─────────┐  │
 │  │  access-api  ├────────────────────────►│  Redis  │  │
 │  │   (Handler)  │◄────────────────────────┤  (Mock) │  │
 │  └──────┬───────┘         False/True      └─────────┘  │
 │         │                                              │
 │         │ publish()                                    │
 │         ▼                                              │
 │  ┌──────────────┐   (Kafka 斷線死線)     ┌───────────┐ │
 │  │  RetryLoop   ├────────────────────────►│FileOutbox │ │
 │  │  (Go Routine)│                         │ (本地硬碟)│ │
 │  └──────────────┘                         └───────────┘ │
 └────────────────────────────────────────────────────────┘

    圖上一定要標記的細節標籤（呼應你的文件）：

        report-api 測試：標註 pdf_visual_test.go。測試當 PDF 渲染遇到邊界條件、非同步匯出工作（Export Jobs）在 Pending -> Processing -> Completed 狀態扭轉時是否完全精確。

        access-api & admin-api 測試：標註 Outbox Pattern 備援機制。測試當 Kafka 徹底斷線時，系統是否能原子性（Atomicity）地將打卡紀錄安全寫入本地硬碟（FileOutbox）。

        cache-invalidation-worker 測試：測試對 Redis 叢集的 Ping 檢測，以及當收到損壞的 JSON 時，Offset 是否防卡死。

第二張圖：整合測試 (Integration Testing) ——「端到端事件流落盤驗證」

整合測試的本質與單元測試相反，它是「完全不使用 Mock，直接對真實架起的 Docker 容器群發起測試」。這裡你要把你們寫的那個極度硬核的 verify-pipeline.sh 腳本，畫成一張 「非同步時間軸同步流向圖」。
1. 你的簡報圖應該這樣畫：

利用帶有順序的時間軸，把你們的第六階段（E2E & Pipeline Validation）文字圖像化：

 【 步驟 1：觸發 】      【 步驟 2：非同步緩衝 】    【 步驟 3：異步落盤 】     【 步驟 4：自動化校驗 】
  make test-e2e        verify-pipeline.sh                     
 ┌──────────────┐        ┌──────────────┐        ┌──────────────┐        ┌──────────────────┐
 │  access-api  ├───────►│  Kafka Queue ├───────►│  ClickHouse  │◄───────┤verify-pipeline.sh│
 │ (POST /swipe)│ 200 OK │(inout-events)│ 批次寫入│(inout_events)│ 2秒輪詢│  (Bash Script)   │
 └──────┬───────┘        └──────────────┘        └──────────────┘        └────────┬─────────┘
        │                                                                         │
        └─────────────────────────── [ 嚴格比對 EventID ] ────────────────────────┘
                                     (驗證資料完全無損、順暢落盤)

    圖上一定要標記的細節標籤：

        動態健康檢查迴圈（Bash 腳本）：標註「最高重試 30 次，每 5 秒輪詢檢查中間件健康度，徹底解決 Flaky Tests（測試隨機失敗） 痛點」。

        非同步輪詢機制：腳本每 2 秒去 ClickHouse 撈取資料，最多等待 30 秒，嚴格比對落盤資料的 id、direction 與 status 是否與 API 回傳的完全一致。